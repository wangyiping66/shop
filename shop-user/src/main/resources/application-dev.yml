# 本地环境配置信息
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9080/eureka/
  instance:
    lease-expiration-duration-in-seconds: 5
    lease-renewal-interval-in-seconds: 10
spring:
  datasource:
    url: jdbc:mysql://192.168.10.56:3306/test1?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  jpa:
    show-sql: true
    format-sql: true
    hibernate:
      ddl-auto: none


  jackson:
    time-zone: GMT+8
